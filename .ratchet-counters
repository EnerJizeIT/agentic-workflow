files_over_400	git ls-files --cached --others --exclude-standard '*.py' | xargs wc -l | awk '$1>400 && $2!="total"' | wc -l
# FU-21 D1: BACKLOG.md is excluded — it carries intentional TODO-NNNN
# identifiers (backlog item references), and every docs reword there
# shifted the count (see "reword to keep the todos ratchet at baseline").
# RUN3-2026-09-22 (#4/#5): baseline raised 2641 -> 2794 — the hygiene unit
# adds 156 TODO-NNNN identifiers (84 in tests/unit/test_todo_hygiene.py,
# 19 in test_cli_entrypoints.py, the rest in code/docs). Test ids are the
# same class as the 1957 already-counted ones in tests/ — not debt.
# RUN3-2026-09-22 (#1): baseline raised 2794 -> 2796 — the named-pipelines
# unit adds 2 TODO-NNNN test ids in tests/unit/test_pipelines_api.py
# (run-state queue/current in the run_brief test). Same class as above.
# RUN3-2026-09-22 (#2): baseline raised 2796 -> 2855 — the per-item-pipeline
# unit adds 59 TODO-NNNN test ids + doc references (12 queue-pipeline tests
# in tests/unit/test_run.py, 4 dispatch/contract pipeline tests in
# tests/unit/test_unit_contract.py, USAGE EN/RU + unit-contract wording).
# Test ids are the same class as the already-counted ones in tests/ — not debt.
# RUN3-2026-09-22 (#2, QA): baseline raised 2855 -> 2861 — the
# refused-launch-no-side-effects regression test adds 6 TODO mentions
# (5 test lines in tests/unit/test_run.py, 1 code comment in
# awf/api/run.py). Same class as above.
# RUN3-2026-09-22 (#6): baseline raised 2861 -> 2868 — the
# single-launch no_checkpoints unit adds 7 TODO-0001 test ids in
# tests/integration/test_plan_checkpoint.py (gate + plumbing fixtures).
# Same class as above: test identifiers, not debt.
# RUN3-2026-09-22 (#7): baseline raised 2868 -> 2884 — the
# live-project phase unit adds 16 TODO-bearing lines: unit-id
# references in awf/phase.py comments/docstrings, test docstrings, and
# TODO-0001/TODO.md archive fixtures in tests/unit/test_phase_live.py
# and tests/agent_workflow_ui/test_awf_tools.py.
# Same class as above: test identifiers, not debt.
# RUN4-2026-09-22 (#2): baseline raised 2884 -> 2886 — the
# feedback-contour unit adds 2 TODO-0001 test ids in
# tests/unit/test_feedback.py (run-state fixture for the header fact).
# Same class as above: test identifiers, not debt.
# RUN4-2026-09-22 (#1): baseline raised 2886 -> 2911, files_over_400
# raised 46 -> 48 — the brief unit (TODO-0051) adds 25 TODO-NNNN
# identifiers (unit-id references in docstrings/comments of awf/brief.py,
# awf/api/brief.py, awf/cmd_brief.py, awf/data/tool_map.yaml,
# tools/awf.py, server.py; TODO-0051/TODO-0042/TODO-0001 test ids in
# tests/unit/test_brief.py; the TODO-0051.md dispatch file) and two files
# over 400 lines (awf/brief.py — card assembly; tests/unit/test_brief.py
# — its test suite). Same class as above: identifiers, not debt.
# RUN5-2026-09-22 (#1): baseline raised 2911 -> 2978 — the leak-gate unit
# (TODO-0052) adds 68 TODO-bearing lines (one removed in awf/verify_pack.py
# docstring renumbering): TODO-0052/TODO-0001/TODO-0002 test ids in
# tests/unit/test_reject_files.py (22), tests/unit/test_commit_gate.py (10),
# test_awf_tools.py (+9), test_api.py (+4), test_verify_pack.py (+3), unit-id
# references in awf/reject_files.py (new module, 13), api/pipeline.py,
# api/dispatch.py, api/_results.py, pipeline_engine.py, tools/awf.py.
# Same class as above: test identifiers and unit-id references, not debt.
# RUN5-2026-09-22 (#1) QA: baseline raised 2978 -> 2993 — the run_next
# leak-gate fix adds 15 TODO-bearing test ids: TestRunNextCarryOver in
# tests/unit/test_run.py (TODO-0001/TODO-0002 dispatch+run ids) and
# TestCarryOverLinkLifecycle in tests/unit/test_reject_files.py (TODO-0002).
# The production fix (api/run.py, api/dispatch.py) carries no TODO id.
# Same class as above: test identifiers, not debt.
# RUN5-2026-09-22 (#2): baseline raised 2993 -> 3157, files_over_400
# raised 48 -> 49 — the todo-retire unit (TODO-0053) adds 164 TODO-bearing
# lines: TestRetireTodo in tests/unit/test_todo_hygiene.py (88, battle-case
# ids TODO-0035/TODO-0001/TODO-00010/TODO-0099), TestTodoRetireEntrypoint in
# test_cli_entrypoints.py (13), TestAwfTodoRetire in test_awf_tools.py (12),
# unit-id references in awf/api/hygiene.py docstrings/comments (20), USAGE
# EN/RU (13), tools/awf.py + cli.py + cmd_todo_retire.py + api/__init__.py
# docstrings (15), tool_map/recovery/server/smoke entries (5). Same class as
# above: test identifiers and unit-id references, not debt. The +1 file over
# 400 is tests/unit/test_todo_hygiene.py (259 -> 495 — its test suite, same
# class as tests/unit/test_brief.py in the RUN4 #1 raise).
# RUN5-2026-09-22 (#2) QA: baseline raised 3157 -> 3172 — the QA round adds
# test_retire_restore_retire_roundtrip_keeps_history in
# tests/unit/test_todo_hygiene.py (15 TODO-bearing lines, battle-case id
# TODO-0035) locking the retire → restore → retire cycle: suffixed copies,
# two RETIRED notes, no overwrite. Same class as above: test identifiers,
# not debt.
# RUN5-2026-09-22 (#3): baseline raised 3172 -> 3176 — the release-prep unit
# (TODO-0054) expands the CHANGELOG [Unreleased] section into the full [1.2.0]
# section. The new section adds 5 TODO-bearing lines (the intro's "TODO
# retirement", the per-item-pipeline example TODO-0023 + "the TODO's
# front-matter", state-hygiene's "a re-issued TODO" / "a TODO that never
# started", the brief's "active TODOs", the retire's "a rejected/abandoned
# TODO"), replacing the old [Unreleased]'s one such line (the brief's "active
# TODOs", kept verbatim). Net +4. Same class as above: unit identifiers in the
# changelog, not debt.
# RUN6-2026-09-22 (#1): baseline raised 3176 -> 3215, files_over_400
# raised 49 -> 51 — the done-after-approve unit (TODO-0056) adds 39
# TODO-bearing lines: TODO-0001/TODO-0002 test ids in
# tests/unit/test_wait_event.py (19, TestDoneCycleDetection fixtures),
# unit-id references in awf/api/wait_event.py docstrings/comments (13),
# the done-event wording in tools/awf.py (3), the done line in USAGE EN/RU
# (1+1), the brief ritual + recovery.md recipe (1+1), the brief's
# "with the TODO" phrasing (1). Same class as above: test identifiers and
# unit-id references, not debt. The +2 files over 400 are
# awf/api/wait_event.py (259 -> 426 — the done-detection helpers) and
# tests/unit/test_wait_event.py (335 -> 524 — its test suite), same class
# as the test_brief.py / test_todo_hygiene.py raises above.
# RUN6-2026-09-22 (#2): baseline raised 3215 -> 3235 — the
# actual-pipeline unit (TODO-0057) adds 20 TODO-bearing lines, one
# removed: 16 test ids in tests/unit/test_dashboard.py
# (TestActualPipeline: TODO-0010/TODO-0009/TODO-0001 queue/state
# fixtures + the contract-block TODO-0010.md fixture), 3
# docstring/comment references in awf/api/dashboard.py (the
# _display_pipeline_name chain wording), and the pipeline-chip line in
# USAGE EN/RU (1+1, the chip is described "next to the TODO"); the
# removed line is the old "# TODO id + worker status" comment in
# generate_state_dict.
# Same class as above: test identifiers and unit-id references, not debt.
# RUN6-2026-09-22 (#3): baseline raised 3235 -> 3242 — the
# quiet-run-status + wait-cap unit (TODO-0058) adds 7 TODO-bearing lines:
# 6 test ids in tests/unit/test_api.py (TestGetStatus::test_conflict_warning_
# neutral_in_active_run / _stale_outside_run — TODO-000n .ready/.md/queue
# fixtures + the "4 active TODOs" assertion) and 1 production message line
# in awf/api/lifecycle.py (the neutral run-queue warning names "each TODO is
# pinned by awf_run_next"). Same class as above: test identifiers and
# message wording, not debt.
# RUN6-2026-09-22 (#4): baseline raised 3242 -> 3377, files_over_400
# raised 51 -> 52 — the todo-update + tree-sha-MCP unit (TODO-0059) adds
# 135 TODO-bearing lines: TODO-0001/0002/0007/0010/0035/0099 test ids in
# tests/unit/test_todo_hygiene.py (TestUpdateTodo, 67),
# tests/agent_workflow_ui/test_awf_tools.py (TestAwfTodoUpdate +
# TestAwfTreeSha, 11), tests/integration/test_cli_entrypoints.py
# (TestTodoUpdateEntrypoint, 21); unit-id references in awf/api/hygiene.py
# (update_todo docstring, 9), awf/api/_results.py (1),
# awf/cmd_todo_update.py (new module, 4), awf/cli.py (help, 3),
# tools/awf.py (awf_todo_update/awf_tree_sha docstrings, 4),
# server.py (registration comment, 1), tool_map.yaml (1), USAGE EN/RU
# (6+6), test_server_smoke.py (expected-tools comment, 1). Same class as
# above: test identifiers and unit-id references, not debt. The +1 file
# over 400 is awf/api/hygiene.py (379 -> 479 — the update_todo helper),
# same class as the wait_event.py / brief.py raises above.
# RUN6-2026-09-22 (#5): baseline raised 3377 -> 3407 — the
# supervisor-onboarding unit (TODO-0060) adds 30 TODO-bearing lines
# (net, after the compressed tool_map.yaml / recovery.md rewrites
# removed a few): new files SKILL.awf-supervisor.md (8 — scenario
# wording), agents_md.py (3), brief_defaults.yaml (2), scenarios.yaml
# (1), skill_installer.py (2, unit-id comment), pyproject.toml (1,
# comment), test_agents_md.py (1), test_supervisor_skill.py (2),
# test_tool_descriptions.py (1); changed: tools/awf.py (next_action
# messages + docstrings), awf/brief.py (next_action_from_status +
# unit-id comments), TestNextActionSmoke in test_awf_tools.py
# (TODO-0001/TODO-0002 ids), test_brief.py (TODO-0042/TODO-0009 test
# ids). Same class as above: test identifiers and unit-id references,
# not debt.
# RUN7-2026-09-23 (#1): baseline raised 3407 -> 3435 — the
# no-false-done-on-salvage-leftover unit (TODO-0061) adds 29 TODO-bearing
# lines (one removed in the _cycle_done_event docstring reword):
# TODO-0001/TODO-0002/TODO-0056/TODO-0061 test ids in
# tests/unit/test_wait_event.py (23 — TestSalvageKillLeftover battle-case
# fixtures + TestStateIsExited state shapes) and unit-id references in
# awf/api/wait_event.py comments/docstrings (6 — the _state_is_exited
# docstring, the _cycle_done_event gate comment, the wait_for_event
# 'done' bullet). Same class as above: test identifiers and unit-id
# references, not debt.
# RUN7-2026-09-23 (#2): baseline raised 3435 -> 3468 — the
# retry-stage-pin unit (TODO-0062) adds 33 TODO-bearing lines:
# TODO-0001/0002/0003 test ids in tests/unit/test_pipeline_engine.py
# (17 — TestP2RetryStage pin/fallback/warning cases) and
# TODO-0001/TODO-0002 test ids in tests/unit/test_continue_resume.py
# (7 — TestContinuePipelineResume::test_explicit_todo_pin_overrides_
# state_and_newest); unit-id references in awf/api/pipeline.py
# (5 — the retry_stage/continue_pipeline pin docstrings with the
# RUN5 incident TODO-0052/TODO-0054) and the "pinned TODO" wording
# in USAGE EN/RU (1+1), tools/awf.py docstring (1), recovery.md
# recipe (1). Same class as above: test identifiers and unit-id
# references, not debt.
# RUN8-2026-09-23 (#1): baseline raised 3468 -> 3520, files_over_400
# raised 52 -> 53 — the ack-pin unit (TODO-0063) adds 52 TODO-bearing
# lines: TODO-0001/0002/0003/0099 test ids in
# tests/unit/test_continue_resume.py (39 — TestAckPinsUnit battle-case
# fixtures + the class docstring's TODO-0034/TODO-0037 incident ids),
# tests/agent_workflow_ui/test_awf_tools.py (6 — TestAwfContinue
# TODO-0007 proxy test + the TODO-0063 unit reference); net +5 in
# awf/api/pipeline.py (the RUN8 #1 (TODO-0063) ack-pinning
# comments/docstrings with the TODO-0034/TODO-0037 incident wording) and
# net +2 in tools/awf.py (the awf_continue ack/todo_id docstrings).
# Same class as above: test identifiers and unit-id references, not debt.
# The +1 file over 400 is tests/unit/test_continue_resume.py
# (337 -> 491 — its test suite), same class as the
# test_wait_event.py / test_brief.py raises above.
# RUN8-2026-09-23 (#2): baseline raised 3520 -> 3527, files_over_400
# raised 53 -> 54 — the kill-kills-worker unit (TODO-0064) adds 7
# TODO-bearing lines, all in the new tests/integration/test_kill_worker.py
# (unit id in the module docstring, the TODO-0064 test ids: worker_todo
# state fixture, the inbox TODO-0064.md/.ready fixtures of the
# start-message test). Same class as above: test identifiers, not debt.
# The +1 file over 400 is that same test file (417 — its test suite),
# same class as the test_continue_resume.py / test_wait_event.py raises
# above.
# RUN9-2026-09-23 (#4): baseline raised 3531 -> 3532 — the salvage-flake
# unit (TODO-0068) adds 1 TODO-bearing line: the TODO-0068 unit reference
# in the fixed test's docstring (tests/unit/test_salvage_signal.py,
# test_verify_still_works_after_salvage_fix). Same class as above: unit
# identifier, not debt.
# RUN9-2026-09-23 (#6): baseline raised 3532 -> 3535, files_over_400
# raised 54 -> 55 — the atomic-rotation unit (TODO-0070) adds 3
# TODO-bearing lines: the TODO-0070 unit reference in the new module
# docstring (tests/unit/test_log_rotation.py) and in awf/_log.py (the
# DEFAULT_LOG_ARCHIVE_KEEP comment), plus the TODO-0001 worker-log
# fixture name in the prune decoy (tests/unit/test_log_rotation.py).
# Same class as above: unit identifiers and test fixtures, not debt.
# The +1 file over 400 is tests/unit/test_log_reader.py (400 -> 410 —
# the _archive_files helper after the archive-name change), same class
# as the test suites in the raises above.
# RUN10-2026-09-24 (#1): baseline raised 3535 -> 3568 — the
# run-hints + phase + false-done-fuse unit (TODO-0071) adds 33
# TODO-bearing lines: TODO-0001/TODO-0002/TODO-0009 test ids in
# tests/unit/test_wait_event.py (TestRunDoneFuse battle-case fixtures),
# tests/unit/test_phase.py (TestDetectPhaseRunActive),
# tests/unit/test_run.py (TestRunIsActive) and
# tests/agent_workflow_ui/test_awf_tools.py (the updated
# run-mode next_action tests); "TODO"-bearing wording in
# awf/api/wait_event.py, awf/phase.py, awf/run_state.py comments/
# docstrings and tools/awf.py docstrings. Same class as above: test
# identifiers and unit-id references, not debt.
# RUN10-2026-09-24 (#3): baseline raised 3568 -> 3579 — the
# metrics-project-scope unit (TODO-0073) adds 11 TODO-bearing lines,
# all in tests/unit/test_metrics.py: make_db_two_projects worker-title
# fixtures (TODO-0001/TODO-0009/TODO-0005 session titles, 3 lines) and
# TestRun10ProjectScope assertions/comments naming those ids (8 lines).
# Same class as above: test identifiers, not debt.
# RUN10-2026-09-24 (#3, QA): baseline raised 3579 -> 3580 — QA review
# adds one supervisor-gate test (test_foreign_supervisor_excluded_by_
# default); its comment names the TODO-0001 baseline offset (1 line).
# Same class: test identifier reference, not debt.
# RUN10-2026-09-24 (#4): baseline raised 3580 -> 3643 — the
# untracked-visibility + include_untracked unit (TODO-0074) adds 63
# TODO-bearing lines: TODO-0042/TODO-0041 test ids in
# tests/unit/test_api.py (TestDispatchIncludeUntracked, 31),
# TODO-0074 unit references in tests/unit/test_verify_pack.py (6),
# tests/unit/test_commit_gate.py (5), tests/agent_workflow_ui/
# test_awf_tools.py (4, the TestDispatchIncludeUntrackedParam ids),
# TODO-0074 references in awf/api/dispatch.py (6), awf/verify_pack.py
# (3), awf/api/pipeline.py (2), awf/api/run.py (1), awf/api/_results.py
# (1), awf/include_untracked.py (new module, 1), tools/awf.py (1) and
# USAGE EN/RU (1+1). Same class as above: test identifiers and unit-id
# references, not debt.
# RUN10-2026-09-24 (#5): baseline raised 3643 -> 3648 — the kill-fuses
# unit (TODO-0075) adds 5 TODO-bearing lines, all in
# tests/integration/test_kill_worker.py: the two new class docstrings
# (TestKillSelfPipelineRefusal / TestLateWorkerSweep unit references)
# and the fake-pipeline worker fixtures (two worker_todo='TODO-0075'
# state writes + the stub opencode prompt 'TODO-0075'). Same class as
# above: test identifiers and fixtures, not debt.
# RUN10-2026-09-24 (owner): BACKLOG.md is open-work only; closed history
# moved to BACKLOG-archive.md. The archive carries intentional TODO-NNNN
# unit ids — excluded from the todos counter for the same reason as
# BACKLOG.md (FU-21 D1).
# 2026-09-25 (stabilization): docs/audit-* — отчёты аудита и планы программ
# (аудит 2026-09-25 добавляет 64 упоминания TODO-NNNN), не долг. Та же
# причина, что у BACKLOG (FU-21 D1). Счётчик с исключением = baseline 3581.
# 2026-09-25 (stabilization, runner_dir): baseline raised 3581 -> 3586 —
# the engine-pin unit (TODO-0077) adds 5 TODO-0077 lines: docstring +
# comment in awf/api/_background.py (2), module docstring in
# tests/unit/test_runner_dir.py (1), the engine-pin paragraph in USAGE EN/RU
# (1+1). Same class as above: unit identifier, not debt.
# 2026-09-25 (stabilization, A-11): baseline raised 3586 -> 3596 — the
# re-init-preserves-runtime unit (TODO-0078) adds 10 TODO-bearing lines:
# 6 in the new tests/negative/test_audit25_a11_init.py (module docstring +
# TODO-0001 inbox/done/state fixtures) and net +4 in
# tests/unit/test_api.py (+2, the renamed preservation test's
# TODO-0001/DONE-TODO-0001 asserts), tests/negative/test_audit12_incidents.py
# (+1, the A-11 pin comment + DONE/TODO.md preservation asserts),
# awf/api/lifecycle.py (+1, the A-11 docstring). Same class as above:
# test identifiers and unit-id references, not debt.
# 2026-09-25 (stabilization, A-12): baseline raised 3596 -> 3628 — the
# id-collision unit (TODO-0079) adds 32 TODO-bearing lines: 31 in the new
# tests/negative/test_audit25_a12_ids.py (module docstring + the
# TODO-0001 inbox/done/outbox fixtures of the four tests) and +1 in
# awf/api/lifecycle.py (the A-12 restore-refusal comment). Same class as
# above: test identifiers and unit-id references, not debt.
# 2026-09-25 (stabilization, A-12, REVIEW F1): baseline raised
# 3628 -> 3638 — the versioning rework of the same unit adds 10
# TODO-bearing lines: the rewritten test_archive_into_occupied_done_
# preserves_both and the new test_restore_rerun_rearchive_cycle_
# completes in tests/negative/test_audit25_a12_ids.py (+9 — TODO-0001
# inbox/done/outbox fixtures), plus a net +1 in awf/todos.py
# (the A-12/REVIEW-F1 docstring). Same class as above: test identifiers
# and unit-id references, not debt.
# 2026-09-25 (stabilization, A-12, REVIEW F2): baseline raised
# 3638 -> 3644 — the QA P1 regression test
# test_archive_two_different_sources_both_preserved adds 6
# TODO-bearing lines in tests/negative/test_audit25_a12_ids.py
# (the DONE-TODO-NNNN.md docstring line + the TODO-0001
# inbox/outbox/archive fixtures). Same class as above: test
# identifiers, not debt. The production fix (awf/todos.py —
# the _suffixed_name helper + the reserved canonical name in the
# versioning loop) carries no TODO id.
# 2026-09-25 (stabilization, A-01, REVIEW fix): baseline raised
# 3644 -> 3648 — the commit-gate index unit (TODO-0080) adds 4
# TODO-bearing lines, all in the new
# tests/negative/test_audit25_a01_index.py: the module docstring
# (TODO-0080 reference) + the three "awf(implement): TODO-0080"
# commit-message fixtures (one per test, the commit message awf
# itself would write). Same class as above: test identifiers and
# fixtures, not debt. The production fix (awf/commit_gate.py —
# index check + targeted unstage) carries no TODO id.
# 2026-09-25 (stabilization, A-13): baseline raised 3650 -> 3669,
# files_over_400 raised 55 -> 56 — the run-CAS unit (TODO-0082) adds
# 19 TODO-bearing lines, all in the new
# tests/negative/test_audit25_a13_run_cas.py: the module docstring
# (unit reference) + the TODO-0001/TODO-0002 queue fixtures of the
# two concurrency tests and the four regressions. Same class as
# above: test identifiers, not debt. The +1 file over 400 is
# awf/run_state.py (354 -> 442 — the A-13 CAS primitives
# generation_of / cas_match / update_run_cas with their docstrings),
# same class as the wait_event.py production-module raise above.
# 2026-09-25 (stabilization, A-21): baseline raised 3669 -> 3689 — the
# refused-launch-signal unit (TODO-0083) adds 20 TODO-bearing lines: 19 in
# the new tests/negative/test_audit25_a21_refused_signal.py (module
# docstring + the TODO-0001 inbox fixtures and assertions of the four
# tests) and +1 in awf/api/run.py (the A-21 rollback comment naming the
# unlaunched TODO). Same class as above: test identifiers and a
# unit comment, not debt. The production fix (awf/api/run.py — had_ready
# snapshot + unlink on the refused branch) carries no TODO id in code.
# 2026-09-26 (stabilization, A-04): baseline raised 3689 -> 3709,
# files_over_400 raised 56 -> 57 — the reject/approve verdict-signal unit
# (TODO-0084) adds 20 TODO-bearing lines: 18 in the new
# tests/negative/test_audit25_a04_verdict_signal.py (module docstring +
# the TODO-0001 inbox/outbox/run fixtures and assertions of the four
# tests) and 2 in production (the approve-conflict message naming the
# TODO in awf/api/pipeline.py, the _verdict_refusal docstring line in
# awf/commit_gate.py). Same class as above: test identifiers and
# unit comments, not debt. The +1 file over 400 is awf/commit_gate.py
# (359 -> 413 — the A-04 _verdict_refusal helper), same class as the
# run_state.py / wait_event.py production-module raises above.
# 2026-09-26 (stabilization, A-15): baseline raised 3709 -> 3718 — the
# verified-sha commit-window unit (TODO-0085) adds 9 TODO-bearing lines,
# all in the new tests/negative/test_audit25_a15_verified_sha.py: the
# TODO = "TODO-0001" constant and its eight use-sites in the three tests
# (approve_commit / maybe_commit calls, VERIFIED path asserts). Same class
# as above: test identifiers, not debt. The production fix
# (awf/commit_gate.py — _verified_refusal + the two check points,
# awf/api/pipeline.py — the VERIFIED-before-signal order) carries no TODO
# id in code.
# 2026-09-26 (stabilization, A-09): baseline raised 3718 -> 3725 — the
# stage-fingerprint unit (TODO-0086) adds 7 TODO-bearing lines, all in the
# new tests/negative/test_audit25_a09_fingerprint.py: the
# TODO = "TODO-0086" constant and its six use-sites (work_fingerprint
# calls in the three tests, the BASELINE-{TODO}.untracked fixture). Same
# class as above: test identifiers, not debt. The production fix
# (awf/verify.py — --binary diff + untracked name/content hash) carries
# no TODO id in code.
# 2026-09-26 (stabilization, R-03): baseline raised 3725 -> 3747 — the
# CommitPlan/isolated-index unit (TODO-0087) adds 22 TODO-bearing lines:
# 14 in the new tests/negative/test_audit25_r03_commit_plan.py (the
# TODO = "TODO-0001" constant and its use-sites in the three tests), 1+2+1
# in the updated test_audit25_a01_index.py / test_audit14_incidents.py /
# test_orchestrator_handlers.py (R-03/A-14 regression assertions), and 6 in
# production (awf/commit_plan.py docstrings, awf/pipeline_engine.py R-03/A-14
# comments; awf/commit_gate.py -2). Same class as above: test identifiers
# and unit comments, not debt. The unit also drops awf/commit_gate.py below
# 400 lines (504 -> 275, the A-01 helpers left with the isolated index), so
# files_over_400 is locked in at 56 (was 57).
# 2026-09-26 (stabilization, R-03, REVIEW attempt 2): baseline raised
# 3747 -> 3751, files_over_400 raised 56 -> 57 — the P2/P3 fix
# (TODO-0087 attempt 2) adds 4 TODO-bearing lines, all in
# tests/negative/test_audit25_r03_commit_plan.py: the two new tests'
# TODO-0001 use-sites (the wait-window test's VERIFIED/APPROVE fixture
# paths and its maybe_commit call, the rename-corner test's
# build_commit_plan call). Same class as above: test identifiers, not
# debt. The +1 file over 400 is awf/commit_plan.py (380 -> 411 — the
# _read_verified_sha helper + the refresh_verified_sha plan update that
# closes the P2 approve-in-wait window), same class as the
# run_state.py / wait_event.py production-module raises above.
# 2026-09-26 (stabilization, D-01/R-07): baseline raised 3751 -> 3763 —
# the single-.include-parser unit (TODO-0088) adds 12 TODO-bearing lines,
# all in the new tests/negative/test_audit25_d01_include_parser.py: the
# TODO-0001 fixture ids of the five tests (the helper-parity pins and the
# run_next wiring/behavior tests). Same class as above: test identifiers,
# not debt. The production change (awf/api/run.py — read_include_list call
# instead of the manual parse) carries no TODO id.
# 2026-09-26 (stabilization, A-17): baseline raised 3803 -> 3818 — the
# commit-stages unit (TODO-0090) adds 15 TODO-bearing lines: 13 in the new
# tests/negative/test_audit25_a17_commit_stages.py (module docstring + the
# "awf(execute)/awf(verify): TODO-0001" commit-subject fixtures and the
# TODO-0001 window asserts) and net +2 in awf/metrics.py (the
# _todo_id_from_stage_commit docstring lines; the collect_commit_info and
# module-docstring reword is TODO-line-for-TODO-line). Same class as above:
# test identifiers and unit-id references, not debt. The production fix
# (awf/metrics.py — _AWF_STAGE_HEAD_RE + _todo_id_from_stage_commit, the
# stage-agnostic collect_commit_info) carries no TODO id in code.
# RUN11-2026-09-26 (TODO-0091): baseline raised 3818 -> 3857 — the
# metrics-contract unit adds 39 TODO-bearing lines: 36 in the new
# tests/negative/test_metrics_contract.py (module docstring + the
# TODO-0001/TODO-0002 session-title fixtures and the unit-id asserts of
# the scope/zero-value tests), 2 in docs/contracts/metrics.md (the
# A-10 boundary wording, the A-17 subject format) and 1 in
# scripts/instruction-gates.conf (the limit-raise comment). Same class
# as above: test identifiers and contract wording, not debt.
todos	git ls-files --cached --others --exclude-standard | grep -v '^\.ratchet' | grep -v '^BACKLOG\.md$' | grep -v '^BACKLOG-archive\.md$' | grep -v '^docs/audit-' | xargs grep -cI TODO | awk -F: '{s+=$NF} END{print s+0}'
