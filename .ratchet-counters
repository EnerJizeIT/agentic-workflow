files_over_400	git ls-files --cached --others --exclude-standard '*.py' | xargs wc -l | awk '$1>400 && $2!="total"' | wc -l
# FU-21 D1: BACKLOG.md is excluded — it carries intentional TODO-NNNN
# identifiers (backlog item references), and every docs reword there
# shifted the count (see "reword to keep the todos ratchet at baseline").
# RUN3-2026-09-22 (#4/#5): baseline raised 2641 -> 2794 — the hygiene unit
# adds 156 TODO-NNNN identifiers (84 in tests/unit/test_todo_hygiene.py,
# 19 in test_cli_entrypoints.py, the rest in code/docs). Test ids are the
# same class as the 1957 already-counted ones in tests/ — not debt.
# RUN3-2026-09-22 (#1): baseline raised 2794 -> 2796 — the named-pipelines
# unit adds 2 TODO-NNNN test ids in tests/unit/test_pipelines_api.py
# (run-state queue/current in the run_brief test). Same class as above.
# RUN3-2026-09-22 (#2): baseline raised 2796 -> 2855 — the per-item-pipeline
# unit adds 59 TODO-NNNN test ids + doc references (12 queue-pipeline tests
# in tests/unit/test_run.py, 4 dispatch/contract pipeline tests in
# tests/unit/test_unit_contract.py, USAGE EN/RU + unit-contract wording).
# Test ids are the same class as the already-counted ones in tests/ — not debt.
# RUN3-2026-09-22 (#2, QA): baseline raised 2855 -> 2861 — the
# refused-launch-no-side-effects regression test adds 6 TODO mentions
# (5 test lines in tests/unit/test_run.py, 1 code comment in
# awf/api/run.py). Same class as above.
# RUN3-2026-09-22 (#6): baseline raised 2861 -> 2868 — the
# single-launch no_checkpoints unit adds 7 TODO-0001 test ids in
# tests/integration/test_plan_checkpoint.py (gate + plumbing fixtures).
# Same class as above: test identifiers, not debt.
# RUN3-2026-09-22 (#7): baseline raised 2868 -> 2884 — the
# live-project phase unit adds 16 TODO-bearing lines: unit-id
# references in awf/phase.py comments/docstrings, test docstrings, and
# TODO-0001/TODO.md archive fixtures in tests/unit/test_phase_live.py
# and tests/agent_workflow_ui/test_awf_tools.py.
# Same class as above: test identifiers, not debt.
# RUN4-2026-09-22 (#2): baseline raised 2884 -> 2886 — the
# feedback-contour unit adds 2 TODO-0001 test ids in
# tests/unit/test_feedback.py (run-state fixture for the header fact).
# Same class as above: test identifiers, not debt.
# RUN4-2026-09-22 (#1): baseline raised 2886 -> 2911, files_over_400
# raised 46 -> 48 — the brief unit (TODO-0051) adds 25 TODO-NNNN
# identifiers (unit-id references in docstrings/comments of awf/brief.py,
# awf/api/brief.py, awf/cmd_brief.py, awf/data/tool_map.yaml,
# tools/awf.py, server.py; TODO-0051/TODO-0042/TODO-0001 test ids in
# tests/unit/test_brief.py; the TODO-0051.md dispatch file) and two files
# over 400 lines (awf/brief.py — card assembly; tests/unit/test_brief.py
# — its test suite). Same class as above: identifiers, not debt.
# RUN5-2026-09-22 (#1): baseline raised 2911 -> 2978 — the leak-gate unit
# (TODO-0052) adds 68 TODO-bearing lines (one removed in awf/verify_pack.py
# docstring renumbering): TODO-0052/TODO-0001/TODO-0002 test ids in
# tests/unit/test_reject_files.py (22), tests/unit/test_commit_gate.py (10),
# test_awf_tools.py (+9), test_api.py (+4), test_verify_pack.py (+3), unit-id
# references in awf/reject_files.py (new module, 13), api/pipeline.py,
# api/dispatch.py, api/_results.py, pipeline_engine.py, tools/awf.py.
# Same class as above: test identifiers and unit-id references, not debt.
# RUN5-2026-09-22 (#1) QA: baseline raised 2978 -> 2993 — the run_next
# leak-gate fix adds 15 TODO-bearing test ids: TestRunNextCarryOver in
# tests/unit/test_run.py (TODO-0001/TODO-0002 dispatch+run ids) and
# TestCarryOverLinkLifecycle in tests/unit/test_reject_files.py (TODO-0002).
# The production fix (api/run.py, api/dispatch.py) carries no TODO id.
# Same class as above: test identifiers, not debt.
# RUN5-2026-09-22 (#2): baseline raised 2993 -> 3157, files_over_400
# raised 48 -> 49 — the todo-retire unit (TODO-0053) adds 164 TODO-bearing
# lines: TestRetireTodo in tests/unit/test_todo_hygiene.py (88, battle-case
# ids TODO-0035/TODO-0001/TODO-00010/TODO-0099), TestTodoRetireEntrypoint in
# test_cli_entrypoints.py (13), TestAwfTodoRetire in test_awf_tools.py (12),
# unit-id references in awf/api/hygiene.py docstrings/comments (20), USAGE
# EN/RU (13), tools/awf.py + cli.py + cmd_todo_retire.py + api/__init__.py
# docstrings (15), tool_map/recovery/server/smoke entries (5). Same class as
# above: test identifiers and unit-id references, not debt. The +1 file over
# 400 is tests/unit/test_todo_hygiene.py (259 -> 495 — its test suite, same
# class as tests/unit/test_brief.py in the RUN4 #1 raise).
# RUN5-2026-09-22 (#2) QA: baseline raised 3157 -> 3172 — the QA round adds
# test_retire_restore_retire_roundtrip_keeps_history in
# tests/unit/test_todo_hygiene.py (15 TODO-bearing lines, battle-case id
# TODO-0035) locking the retire → restore → retire cycle: suffixed copies,
# two RETIRED notes, no overwrite. Same class as above: test identifiers,
# not debt.
# RUN5-2026-09-22 (#3): baseline raised 3172 -> 3176 — the release-prep unit
# (TODO-0054) expands the CHANGELOG [Unreleased] section into the full [1.2.0]
# section. The new section adds 5 TODO-bearing lines (the intro's "TODO
# retirement", the per-item-pipeline example TODO-0023 + "the TODO's
# front-matter", state-hygiene's "a re-issued TODO" / "a TODO that never
# started", the brief's "active TODOs", the retire's "a rejected/abandoned
# TODO"), replacing the old [Unreleased]'s one such line (the brief's "active
# TODOs", kept verbatim). Net +4. Same class as above: unit identifiers in the
# changelog, not debt.
todos	git ls-files --cached --others --exclude-standard | grep -v '^\.ratchet' | grep -v '^BACKLOG\.md$' | xargs grep -cI TODO | awk -F: '{s+=$NF} END{print s+0}'
